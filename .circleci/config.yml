version: 2

jobs:
  scratch:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - checkout
      - run: .circleci/install-minikube.sh


workflows:
  version: 2
  daemon:
    jobs:
      - scratch